pipeline {
    agent any
    stages  {
        stage('build') {
            steps {
                echo "welcome to jenkins environmet"
                sh 'echo using shell within Jenskinsfile'
                echo 'not using shell within Jenkinsfile'
                echo 'hi'
            }
        }
    }
}
